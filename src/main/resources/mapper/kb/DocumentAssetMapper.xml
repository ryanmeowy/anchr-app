<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anchr.core.kb.infrastructure.persistence.DocumentAssetMapper">

    <resultMap id="DocumentAssetRecordMap" type="com.anchr.core.kb.infrastructure.persistence.DocumentAssetRecord">
        <id property="id" column="id"/>
        <result property="workspaceId" column="workspace_id"/>
        <result property="kbId" column="kb_id"/>
        <result property="fileName" column="file_name"/>
        <result property="title" column="title"/>
        <result property="fileType" column="file_type"/>
        <result property="mimeType" column="mime_type"/>
        <result property="sizeBytes" column="size_bytes"/>
        <result property="fileHash" column="file_hash"/>
        <result property="objectKey" column="object_key"/>
        <result property="previewObjectKey" column="preview_object_key"/>
        <result property="thumbnailKey" column="thumbnail_key"/>
        <result property="sourceUrl" column="source_url"/>
        <result property="parseStatus" column="parse_status"/>
        <result property="indexStatus" column="index_status"/>
        <result property="segmentCount" column="segment_count"/>
        <result property="embeddingProfile" column="embedding_profile"/>
        <result property="errorCode" column="error_code"/>
        <result property="errorMessage" column="error_message"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <sql id="DocumentAssetColumns">
        id, workspace_id, kb_id, file_name, title, file_type, mime_type,
        size_bytes, file_hash, object_key, preview_object_key, thumbnail_key,
        source_url, parse_status, index_status, segment_count, embedding_profile,
        error_code, error_message, created_by, updated_by, created_at, updated_at, deleted_at
    </sql>

    <select id="findActiveById" resultMap="DocumentAssetRecordMap">
        select
            <include refid="DocumentAssetColumns"/>
        from document_asset
        where id = #{assetId}
          and workspace_id = #{workspaceId}
          and kb_id = #{kbId}
          and deleted_at is null
    </select>

    <select id="listActive" resultMap="DocumentAssetRecordMap">
        select
            <include refid="DocumentAssetColumns"/>
        from document_asset
        where workspace_id = #{workspaceId}
          and kb_id = #{kbId}
          and deleted_at is null
        order by created_at desc, id desc
        limit #{limit} offset #{offset}
    </select>

    <select id="countActive" resultType="long">
        select count(1)
        from document_asset
        where workspace_id = #{workspaceId}
          and kb_id = #{kbId}
          and deleted_at is null
    </select>

    <select id="findActiveByHash" resultMap="DocumentAssetRecordMap">
        select
            <include refid="DocumentAssetColumns"/>
        from document_asset
        where workspace_id = #{workspaceId}
          and kb_id = #{kbId}
          and file_hash = #{fileHash}
          and deleted_at is null
        order by created_at desc, id desc
        limit 1
    </select>

    <update id="markDeleted">
        update document_asset
        set updated_by = #{updatedBy},
            updated_at = #{updatedAt},
            deleted_at = #{updatedAt}
        where id = #{assetId}
          and workspace_id = #{workspaceId}
          and kb_id = #{kbId}
          and deleted_at is null
    </update>
</mapper>
