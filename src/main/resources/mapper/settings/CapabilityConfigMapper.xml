<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anchr.core.settings.infrastructure.persistence.CapabilityConfigMapper">

    <resultMap id="CapabilityConfigRecordMap" type="com.anchr.core.settings.infrastructure.persistence.CapabilityConfigRecord">
        <id property="id" column="id"/>
        <result property="capability" column="capability"/>
        <result property="baseUrl" column="base_url"/>
        <result property="apiKeyEnc" column="api_key_enc"/>
        <result property="modelName" column="model_name"/>
        <result property="extraConfig" column="extra_config"/>
        <result property="enabled" column="enabled"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="capabilityConfigColumns">
        id, capability, base_url, api_key_enc, model_name,
        extra_config, enabled, updated_by, updated_at
    </sql>

    <select id="findByCapability" resultMap="CapabilityConfigRecordMap">
        select <include refid="capabilityConfigColumns"/>
        from capability_config
        where capability = #{capability}
          and enabled = true
          and deleted_at is null
    </select>

    <select id="findAllByCapability" resultMap="CapabilityConfigRecordMap">
        select <include refid="capabilityConfigColumns"/>
        from capability_config
        where capability = #{capability}
          and deleted_at is null
    </select>

    <select id="findById" resultMap="CapabilityConfigRecordMap">
        select <include refid="capabilityConfigColumns"/>
        from capability_config
        where id = #{id}
          and deleted_at is null
    </select>

    <insert id="insert" parameterType="com.anchr.core.settings.infrastructure.persistence.CapabilityConfigRecord">
        insert into capability_config (
            <include refid="capabilityConfigColumns"/>
        ) values (
            #{id}, #{capability}, #{baseUrl}, #{apiKeyEnc}, #{modelName},
            #{extraConfig}, #{enabled}, #{updatedBy}, #{updatedAt}
        )
    </insert>

    <update id="update" parameterType="com.anchr.core.settings.infrastructure.persistence.CapabilityConfigRecord">
        update capability_config
        set base_url = #{baseUrl},
            api_key_enc = #{apiKeyEnc},
            model_name = #{modelName},
            extra_config = #{extraConfig},
            enabled = #{enabled},
            updated_by = #{updatedBy},
            updated_at = #{updatedAt}
        where id = #{id}
          and deleted_at is null
    </update>

    <update id="select">
        update capability_config
        set enabled = (case when id = #{id} then true else false end)
        where capability = #{capability}
          and deleted_at is null
    </update>

    <update id="disableAll">
        update capability_config set enabled = false
        where capability = #{capability}
          and deleted_at is null
    </update>

    <update id="del">
        update capability_config
        set deleted_at = now()
        where capability = #{capability} and id = #{id}
    </update>
</mapper>
